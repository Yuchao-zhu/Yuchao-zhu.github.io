steps:
  - name: Checkout code
    uses: actions/checkout@v4

  - name: Set up Ruby
    uses: ruby/setup-ruby@v1
    with:
      ruby-version: 3.1.4  # 指定你的 Ruby 版本
      bundler-cache: true  # 自动缓存 Gemfile 依赖

  - name: Build site
    run: bundle exec build-site  # 替换为你的构建命令
